# Optional Gemini integration. Both values are required for a live provider call.
GEMINI_API_KEY=""
GEMINI_MODEL=""

# Public application URL, if needed by the hosting environment.
APP_URL="http://localhost:3000"
PORT="3000"

# Optional Sui deployment identifiers. These are reported as unverified metadata
# unless separate explorer/bytecode evidence is maintained.
QSUI_PACKAGE_ID=""
QSUI_TREASURY_CAP=""
